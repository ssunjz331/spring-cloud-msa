
server:
  port: 8888


spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file://C:/inflearn-msa/spring-cloud-config-repository
        git:
          uri: https://github.com/ssunjz331/spring-cloud-config-repository

